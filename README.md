# Acabou a Pipoca | Linktree

Página pública de links recentes do perfil @acabou_a_pipoca.

Arquivos publicados:

- `docs/index.html`: página estática para GitHub Pages
- `docs/links.json`: dados dos links recentes
- `docs/logo.jpg`: logo exibido no topo da página
- `docs/.nojekyll`: evita processamento Jekyll no GitHub Pages

Configure o GitHub Pages para publicar a branch `main` a partir da pasta `/docs`.
